# Changesets

Each Markdown file here is a changeset: a pending version bump for `render-blueprint` and the line
it adds to `CHANGELOG.md`. `CONTRIBUTING.md` says when to add one and how a release goes out.
