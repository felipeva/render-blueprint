# Generated by render-blueprint. Do not edit this file by hand.
# Edit the TypeScript blueprint that produced it and synthesize again.

previews:
  generation: automatic
  expireAfterDays: 7
services:
  - type: web
    name: api
    region: oregon
    plan: starter
    runtime: node
    repo: https://github.com/acme/mono
    rootDir: apps/api
    buildCommand: pnpm install --frozen-lockfile && pnpm build
    startCommand: pnpm start
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: records
          property: connectionString
      - key: API_BASE_URL
        value: https://api.acme.dev
        previewValue: https://api.preview.acme.dev
    autoDeployTrigger: commit
    ipAllowList:
      - source: 203.0.113.4/30
        description: office range
      - source: 198.51.100.1
        description: ci runner
      - source: 2001:db8:1::/48
        description: office range over v6
      - source: 2001:db8:1::c0a8:1
        description: ci runner over v6
  - type: web
    name: marketing
    runtime: static
    buildCommand: pnpm install --frozen-lockfile && pnpm build
    staticPublishPath: ./dist
    previews:
      generation: 'off'
    rootDir: apps/marketing
    repo: https://github.com/acme/mono
    ipAllowList:
      - source: 198.51.100.0/24
        description: edge
      - source: 198.51.100.7
        description: edge probe
      - source: 2001:db8:2::1
        description: edge over v6
  - type: cron
    name: nightly
    region: oregon
    plan: starter
    runtime: node
    schedule: 0,30 8-18/2 * JAN-DEC MON-FRI
    buildCommand: pnpm install --frozen-lockfile && pnpm build
    startCommand: pnpm report
    repo: https://github.com/acme/mono
    rootDir: apps/reports
  - type: keyvalue
    name: cache
    region: oregon
    ipAllowList:
      - source: 10.0.0.0/8
        description: private network
      - source: 10.1.2.3
        description: admin host
      - source: fd00::/8
        description: private network over v6
    plan: standard
    persistenceMode: journal-snapshot
databases:
  - name: records
    region: oregon
    plan: 1c-2g
    diskSizeGB: 4005
    previewPlan: 1c-2g
    previewDiskSizeGB: 4010
    postgresMajorVersion: '16'
    highAvailability:
      enabled: true
    ipAllowList:
      - source: 203.0.113.4/30
        description: office range
      - source: 198.51.100.1
        description: ci runner
      - source: 2001:db8:1::/48
        description: office range over v6
      - source: 2001:db8:1::c0a8:1
        description: ci runner over v6
